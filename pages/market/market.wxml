
<!-- ? -->
<view>
  <view class="market">
      <petSticker wx:for="{{petSticket}}" wx:for-index="petId" wx:for-item="pet" id="{{petId}}" petMessage="{{pet}}" bind:tap="openDetail"></petSticker>
  </view>
</view>

