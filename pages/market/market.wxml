<view>
  <view class="market">
      <petSticker wx:for="{{petSticket}}" wx:for-index="petId" wx:for-item="pet" data-trade="{{petSticket[petId].trade_id}}" petMessage="{{pet}}" wx:key="photoAddress"
      bind:tap="openDetail"></petSticker>
  </view>
</view>

