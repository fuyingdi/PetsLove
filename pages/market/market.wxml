
<!-- ? -->
<view>
  <view class="market">
      <petStricker wx:for="{{petStricket}}" wx:for-index="petId" wx:for-item="pet" id="{{petId}}" petMessage="{{pet}}" bind:tap="openDetail"></petStricker>
  </view>
</view>

