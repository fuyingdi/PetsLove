api:  
```
1. /post/getPostInfoList  
微信端发送信息格式：  
    [  
        （以下对象共三个）
        {
        post_id  
        avatar(url)  
        nickname(text)  
        outline(text)  
        picture(array)  
        visitor_number  
        }
    ]  
    
```